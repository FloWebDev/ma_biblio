{% extends 'base.html.twig' %}

{% block title %}Modification catégorie{% endblock %}

{% block body %}
<section class="page-section mt-5" id="category_edit">
    <div class="container">
        <h2 class="page-section-heading text-left text-uppercase text-secondary mb-0">Modification catégorie</h2>

    {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('category_index') }}">Retour à la liste</a>

    {# {{ include('category/_delete_form.html.twig') }} #}
</div>
</section>
{% endblock %}
