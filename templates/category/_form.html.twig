{{ form_start(form, {'attr': {'novalidate': 'novalidate', 'class': 'mt-3'}}) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-primary btn-xl" value="Valider">
{{ form_end(form) }}
